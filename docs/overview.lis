let foo (9)
let baz (+ 2 (- 4 2))

format (foo)

cond (= foo baz) {
    format ("hello, world")
} {
    format ("dlrow ,olleh")
}

until (< foo baz) {
    | comment ..
}

fun add (x y) {
    (+ x y)
}